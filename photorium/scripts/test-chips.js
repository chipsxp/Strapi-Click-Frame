const http = require('http'); function post(path, body, token) { return new Promise((resolve, reject) => { const options = { hostname: '127.0.0.1', port: 1337, path, method: 'POST', headers: { 'Content-Type': 'application/json' } }; if (token) options.headers['Authorization'] = 'Bearer ' + token; const req = http.request(options, (res) => { let data = ''; res.on('data', (chunk) => data += chunk); res.on('end', () => { try { resolve({ status: res.statusCode, data: JSON.parse(data) }); } catch(e) { resolve({ status: res.statusCode, data }); } }); }); req.on('error', reject); req.write(JSON.stringify(body)); req.end(); }); } async function test() { console.log('Starting test...'); try { const login = await post('/api/auth/local', { identifier: 'DummyUser2', password: 'Password123' }); const jwt = login.data.jwt; if (!jwt) { console.error('Login failed:', login.data); return; } const photoId = 'zs4g4zgt9cnxsqcc0vzw1p8y'; console.log('1. Giving Classic chip (expect success as last was yesterday)...'); const res1 = await post('/api/reactions/give', { photoId, type: 'classic' }, jwt); console.log(res1.status, res1.data); console.log('\n2. Testing daily limit rejection (again today)...'); const res2 = await post('/api/reactions/give', { photoId, type: 'classic' }, jwt); console.log(res2.status, res2.data); } catch (err) { console.error('Test error:', err); } } test();
